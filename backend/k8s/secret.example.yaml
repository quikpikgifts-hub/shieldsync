# EXAMPLE ONLY — shows the Secret's expected shape. Never `kubectl apply -f` this file:
# replace every value via your cluster's real secrets mechanism (Sealed Secrets, External
# Secrets Operator, SOPS, etc.), not by editing this placeholder in place.
apiVersion: v1
kind: Secret
metadata:
  name: ember-backend-secrets
type: Opaque
stringData:
  DATABASE_URL: "postgresql://REPLACE_ME:REPLACE_ME@REPLACE_ME:5432/ember_production?schema=public"
  REDIS_URL: "redis://:REPLACE_ME@REPLACE_ME:6379/0"
  JWT_ACCESS_SECRET: "REPLACE_ME_with_a_real_random_32_plus_byte_value"
  SMTP_HOST: "REPLACE_ME"
  SMTP_USER: "REPLACE_ME"
  SMTP_PASSWORD: "REPLACE_ME"
  S3_ENDPOINT: "REPLACE_ME_or_omit_for_real_AWS_S3"
  S3_ACCESS_KEY_ID: "REPLACE_ME"
  S3_SECRET_ACCESS_KEY: "REPLACE_ME"
  S3_BUCKET: "REPLACE_ME"
  SENTRY_DSN: "REPLACE_ME"
